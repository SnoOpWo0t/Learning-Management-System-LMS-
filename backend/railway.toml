[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "node --dns-result-order=ipv4first ./node_modules/@strapi/strapi/bin/strapi.js start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
