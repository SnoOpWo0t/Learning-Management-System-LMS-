# 🎥 ১০ মিনিটের পূর্ণাঙ্গ ভিডিও প্রেজেন্টেশন স্ক্রিপ্ট (Full In-Depth Bengali Walkthrough)

> **লক্ষ্য:** এই স্ক্রিপ্টটি অনুসরণ করে আপনি আত্মবিশ্বাসের সাথে ১০ মিনিটের মধ্যে পুরো প্রজেক্ট, ৪টি রোল, লাইভ ডেটা ফ্লো, ব্যাকএন্ড সিকিউরিটি, কোড লজিক এবং ডিপ্লয়মেন্ট সুন্দরভাবে বুঝিয়ে ভিডিও রেকর্ড করতে পারবেন।

---

## ⏱️ সময় বন্টন ও টাইমস্ট্যাম্প রূপরেখা (Time Breakdown)

| পর্ব | বিষয়বস্তু | সময় |
| :--- | :--- | :--- |
| **পর্ব ১** | ভূমিকা, টেক স্ট্যাক এবং লাইভ ডিপ্লয়মেন্ট ওভারভিউ | `0:00 - 1:00` (১ মিনিট) |
| **পর্ব ২** | স্টুডেন্ট জার্নি (Live Student Demo + Progress + Quiz) | `1:00 - 3:00` (২ মিনিট) |
| **পর্ব ৩** | ইনস্ট্রাক্টর ও কনটেন্ট ম্যানেজার জার্নি (Course, Quiz, Blog) | `3:00 - 4:45` (১ মিনিট ৪৫ সেকেন্ড) |
| **পর্ব ৪** | অ্যাডমিন প্যানেল ও রোল ম্যানেজমেন্ট (Admin Dashboard & Roles) | `4:45 - 6:00` (১ মিনিট ১৫ সেকেন্ড) |
| **পর্ব ৫** | ডেটা ফ্লো ও ব্যাকএন্ড RBAC সিকিউরিটি (Data Flow + DevTools) | `6:00 - 7:15` (১ মিনিট ১৫ সেকেন্ড) |
| **পর্ব ৬** | কোড ওয়াকথ্রু: প্রগ্রেস ট্র্যাকিং এবং কুইজ অটো-গ্রেডিং লজিক (Line by Line) | `7:15 - 8:45` (১ মিনিট ৩০ সেকেন্ড) |
| **পর্ব ৭** | ডিপ্লয়মেন্ট সেটআপ ও সমাপ্তি (Vercel, Railway, PostgreSQL) | `8:45 - 10:00` (১ মিনিট ১৫ সেকেন্ড) |

---

## 🎬 পর্ব ১: ভূমিকা ও টেক স্ট্যাক (`0:00 - 1:00`)

### 🖥️ স্ক্রিনে যা দেখাবেন:
* ব্রাউজারে লাইভ ডিপ্লয়মেন্টের হোমপেজ (`https://learning-management-system-lms-five.vercel.app`).
* ডার্ক/লাইট মোড টগল করে মসৃণ ডিজাইন দেখানো।

### 🗣️ মুখে যা বলবেন (বাংলায়):
> "আসসালামু আলাইকুম / হ্যালো সবাইকে। আজকে আমি আমার তৈরি করা **Next-Generation Learning Management System (LMS)** প্রজেক্টটির একটি কমপ্লিট এন্ড-টু-এন্ড ভিডিও ওয়াকথ্রু উপস্থাপন করছি।
> 
> এই প্রজেক্টটির আর্কিটেকচার পুরোপুরি ডিকাপল্ড (Decoupled):
> * **Frontend:** Next.js 16 (App Router এবং Server Components ব্যবহার করে তৈরি), যা **Vercel**-এ হোস্ট করা আছে।
> * **Backend & CMS:** Strapi 5 (TypeScript), যা **Railway** প্ল্যাটফর্মে ডিপ্লয় করা এবং একটি প্রোডাকশন **PostgreSQL** ডাটাবেজের সাথে কানেক্টেড।
> 
> সিস্টেমটিতে ৪টি স্বতন্ত্র রোল রয়েছে: **Admin**, **Content Manager**, **Instructor**, এবং **Student**। পুরো সিস্টেমে ফ্রন্টএন্ড এবং ব্যাকএন্ড উভয় স্তরেই নিখুঁত Role-Based Access Control (RBAC) নিশ্চিত করা হয়েছে।"

---

## 🎬 পর্ব ২: স্টুডেন্ট রোল — লাইভ ডেমো (`1:00 - 3:00`)

### 🖥️ স্ক্রিনে যা দেখাবেন:
1. হোমপেজ থেকে লগইন পেজে যাওয়া (`/login`)।
2. `student@demo.com` এবং `Password123!` দিয়ে লগইন করা।
3. স্টুডেন্ট ড্যাশবোর্ড (`/dashboard`) এবং যেকোনো একটি কোর্সের পেজ ওপেন করা।
4. **"Continue Learning"** / **"Enroll Now"** বাটনে ক্লিক করে কোর্স প্লেয়ার (`/dashboard/courses/[id]/learn`) এ ঢোকা।
5. ভিডিও দেখা, লেসনে **"Mark as Complete"** টিক দেওয়া এবং ড্যাশবোর্ডে পার্সেন্টেজ আপডেট হওয়া দেখানো।
6. **Quiz** ট্যাবে গিয়ে প্রশ্নগুলোর উত্তর দিয়ে সাবমিট করা এবং কনফেটি অ্যানিমেশনসহ রেজাল্ট দেখানো।
7. পার্সোনাল নোটপ্যাডে কিছু নোট লিখে সেভ করা।

### 🗣️ মুখে যা বলবেন (বাংলায়):
> "এখন আমি একজন **Student** হিসেবে প্ল্যাটফর্মের লাইভ ফ্লো দেখাচ্ছি।
> 
> আমি `student@demo.com` হিসেবে লগইন করলাম। লগইন করার সাথে সাথে আমাকে সরাসরি স্টুডেন্ট ওভারভিউ ড্যাশবোর্ডে নিয়ে আসা হয়েছে। এখানে অ্যাক্টিভ কোর্স, কমপ্লিটেড লেসন, কুইজ স্কোর এবং 'Continue Learning' কার্ড দেখা যাচ্ছে।
> 
> যেকোনো কোর্সে গেলে আমাদের স্মার্ট **EnrollButton** কম্পোনেন্ট স্বয়ংক্রিয়ভাবে ডিটেক্ট করে আমি এনরোল্ড কি না। এনরোল্ড থাকায় এটি আমাকে 'Continue Learning' দেখাচ্ছে।
> 
> কোর্স প্লেয়ারে প্রবেশ করলে বামপাশে স্ট্রাকচার্ড লেসন লিস্ট এবং ডানপাশে রেসপনসিভ ভিডিও প্লেয়ার ও নোটবুক দেখতে পাচ্ছি। আমি যখন একটি লেসন কমপ্লিট করি, তখন ডাটাবেজে তাৎক্ষণিকভাবে প্রগ্রেস আপডেট হয় এবং রিফ্রেশ দিলেও এই প্রগ্রেস ডাটাবেজে পারসিস্ট করে।
> 
> এবার কুইজ সেকশনে আসি। এখানে মাল্টিপল চয়েস প্রশ্ন রয়েছে। মজার বিষয় হলো, ফ্রন্টএন্ডে সঠিক উত্তরের কোনো তথ্য পাঠানো হয় না যাতে DevTools দিয়ে কেউ চিটিং না করতে পারে। আমি উত্তর সিলেক্ট করে সাবমিট করতেই ব্যাকএন্ড স্বয়ংক্রিয়ভাবে মূল্যায়ন করে আমাকে ইনস্ট্যান্ট স্কোর জানিয়ে দিল।"

---

## 🎬 পর্ব ৩: ইনস্ট্রাক্টর ও কনটেন্ট ম্যানেজার জার্নি (`3:00 - 4:45`)

### 🖥️ স্ক্রিনে যা দেখাবেন:
1. স্টুডেন্ট অ্যাকাউন্ট থেকে লগআউট করে `instructor@demo.com` দিয়ে লগইন করা।
2. **Instructor Studio** ড্যাশবোর্ড দেখানো (My Courses, Total Students, Curriculum Lessons)।
3. **Manage Courses** (`/dashboard/manage-courses`) এ গিয়ে ইনস্ট্রাক্টর শুধুমাত্র তার নিজের কোর্স দেখতে ও এডিট করতে পারছে তা দেখানো।
4. **Progress** বাটনে ক্লিক করে এনরোল্ড স্টুডেন্টদের লিস্ট ও প্রগ্রেস দেখানো।
5. লগআউট করে `content@demo.com` দিয়ে লগইন করা।
6. **Content Manager Hub** (`/dashboard`) দেখানো (Content Mix Pie Chart, Course Skill Level Bar Chart)।
7. **Manage Blogs** (`/dashboard/blogs`) এ গিয়ে নতুন আর্টিকেল লেখা, **Draft** থেকে **Published** এ টগল করা এবং পাবলিক `/blog` পেজে গিয়ে লাইভ ব্লগ দেখানো।

### 🗣️ মুখে যা বলবেন (বাংলায়):
> "এবার আমরা **Instructor** এবং **Content Manager** রোল দুটি দেখব।
> 
> প্রথমে আমি `instructor@demo.com` দিয়ে লগইন করলাম। দেখুন, ইনস্ট্রাক্টরের ড্যাশবোর্ড সম্পূর্ণ আলাদা—এখানে তার তৈরি করা কোর্স এবং মোট এনরোল্ড স্টুডেন্ট সংখ্যা দেখাচ্ছে। ইনস্ট্রাক্টর শুধুমাত্র তার নিজের কোর্সের লেসন, কুইজ এবং স্টুডেন্টদের প্রগ্রেস ট্র্যাক করতে পারে। অন্য ইনস্ট্রাক্টরের কনটেন্ট সে দেখতে বা পরিবর্তন করতে পারে না।
> 
> এবার আমি **Content Manager** (`content@demo.com`) হিসেবে লগইন করছি।
> কনটেন্ট ম্যানেজারের ড্যাশবোর্ডে লাইভ অ্যানালিটিক্স চার্ট রয়েছে—যেমন কনটেন্ট মিক্স পাই-চার্ট এবং কোর্স ডিফিকাল্টি বার-চার্ট।
> 
> কনটেন্ট ম্যানেজার 'Manage Blogs' এ গিয়ে খুব সহজেই নতুন আর্টিকেল তৈরি করতে পারেন। এখানে **Draft vs Published** লাইফসাইকেল রয়েছে। যতক্ষণ একটি আর্টিকেল 'Draft' অবস্থায় থাকে, ততক্ষণ পাবলিক বা স্টুডেন্টরা তা দেখতে পায় না। যখনই এটি 'Published' করা হয়, সাথে সাথে পাবলিক `/blog` পেজে লাইভ হয়ে যায়।"

---

## 🎬 পর্ব ৪: অ্যাডমিন প্যানেল ও রোল ম্যানেজমেন্ট (`4:45 - 6:00`)

### 🖥️ স্ক্রিনে যা দেখাবেন:
1. `admin@demo.com` দিয়ে লগইন করা।
2. **Admin Overview** ড্যাশবোর্ড (Total Users, Total Courses, Total Enrollments, User Role Distribution Pie Chart)।
3. **Manage Users** (`/dashboard/admin/users`) পেজে গিয়ে যেকোনো ইউজারকে সিলেক্ট করে তার রোল পরিবর্তন করা (যেমন Student থেকে Instructor বা Content Manager)।
4. কোনো কোর্স ডিলিট করলে যে ক্যাশকেডিং ডিলিট (Cascading Delete) হয়ে সব সম্পর্কিত লেসন, কুইজ ও এনরোলমেন্ট ডাটাবেজ থেকে ক্লিন হয়ে যায় তা দেখানো।

### 🗣️ মুখে যা বলবেন (বাংলায়):
> "এবার আমরা প্ল্যাটফর্মের সর্বোচ্চ ক্ষমতাধর রোল—**Admin** ড্যাশবোর্ডে প্রবেশ করছি।
> 
> অ্যাডমিনের ড্যাশবোর্ডে পুরো সিস্টেমের গ্লোবাল মেট্রিক্স দেখা যায়—যেমন মোট ইউজার সংখ্যা, রোল ডিস্ট্রিবিউশন পাই চার্ট এবং এনরোলমেন্ট গ্রোথ।
> 
> অ্যাডমিন 'Manage Users' প্যানেল থেকে যেকোনো ইউজারের রোল ইনস্ট্যান্টলি পরিবর্তন করতে পারেন।
> এছাড়াও অ্যাডমিনের সম্পূর্ণ প্ল্যাটফর্মের যেকোনো কোর্স বা ব্লগ মডারেট ও ডিলিট করার পারমিশন রয়েছে। আমরা ব্যাকএন্ডে ক্যাসকেডিং ডিলিট লজিক ইমপ্লিমেন্ট করেছি, যার ফলে একটি কোর্স ডিলিট করলে PostgreSQL ডাটাবেজে কোনো Foreign Key এরর ছাড়া তার সাথে সম্পর্কিত সব লেসন, কুইজ, কোয়েশ্চন ও এনরোলমেন্ট ক্লিনভাবে ডিলিট হয়ে যায়।"

---

## 🎬 পর্ব ৫: ডেটা ফ্লো ও ব্যাকএন্ড RBAC সিকিউরিটি (`6:00 - 7:15`)

### 🖥️ স্ক্রিনে যা যা করবেন:
1. ব্রাউজারে স্টুডেন্ট হিসেবে কোর্স প্লেয়ারে গিয়ে **`F12`** চাপুন এবং **`Network`** ট্যাবে যান (Filter: `Fetch/XHR`)।
2. কুইজের ৩টি উত্তর সিলেক্ট করে **"Submit Answers"** বাটনে ক্লিক করুন।
3. Network ট্যাবে আসা `submit` রিকোয়েস্টে ক্লিক করে দেখান:
   * **Headers Tab:** `Authorization: Bearer <token>` এবং Request URL।
   * **Payload Tab:** `{ answers: { ... } }`।
   * **Response Tab:** `{ data: { score: 100, totalQuestions: 3, message: "Quiz submitted successfully!" } }`।
4. এরপর VS Code এ গিয়ে [`backend/src/api/course/controllers/course.ts`](file:///F:/Learning%20Management%20System%20%28LMS%29/backend/src/api/course/controllers/course.ts) ফাইলটি ওপেন করুন।

### 🗣️ মুখে যা বলবেন (বাংলায়):
> "প্রজেক্টের একটি অত্যন্ত গুরুত্বপূর্ণ অংশ হলো **Data Flow** এবং **Backend Security**। আমি নেটওয়ার্ক ট্যাবের মাধ্যমে লাইভ ডেটা ট্রানজিট প্রক্রিয়াটি দেখাচ্ছি।
> 
> ১. **Client Request:** স্টুডেন্ট যখন কুইজের উত্তর সাবমিট করে, তখন Next.js থেকে Strapi ব্যাকএন্ডের `POST /api/quizzes/:id/submit` এন্ডপয়েন্টে রিকোয়েস্ট পাঠানো হয়। রিকোয়েস্ট হেডারে স্টুডেন্টের সিকিউর Bearer JWT টোকেন থাকে এবং বডিতে শুধুমাত্র সিলেক্ট করা অপশনগুলোর আইডি থাকে।
> 
> ২. **Backend Verification:** ব্যাকএন্ড কন্ট্রোলার টোকেন ভ্যালিডেট করে ডাটাবেজ থেকে গোপন সঠিক উত্তরের সাথে মিলিয়ে দেখে এবং সার্ভারেই স্কোর ক্যালকুলেট করে।
> 
> ৩. **Database Save & Response:** ব্যাকএন্ডে `quiz-result` টেবিলে স্কোর পারসিস্ট করা হয় এবং ফ্রন্টএন্ডে রেসপন্স হিসেবে ক্যালকুলেটেড স্কোর ও মেসেজ পাঠানো হয়, যার ফলে স্ক্রিনে সাথে সাথে কনফেটি অ্যানিমেশন ও স্কোর রেন্ডার হয়।
> 
> সিকিউরিটির ক্ষেত্রে আমরা শুধু ফ্রন্টএন্ডে বাটন লুকিয়ে রাখিনি, বরং `course.ts` এবং `quiz.ts` কন্ট্রোলার লেভেলে `getUserRole()` ফাংশনের মাধ্যমে প্রতিটি ডাটাবেজ অপারেশনে কঠোর পারমিশন নিশ্চিত করেছি যাতে কোনো অননুমোদিত ইউজার বা অন্য ইনস্ট্রাক্টরের কনটেন্ট কেউ অ্যাক্সেস বা মডিফাই করতে না পারে।"

---

## 🎬 পর্ব ৬: কোড ওয়াকথ্রু — প্রগ্রেস ট্র্যাকিং ও কুইজ গ্রেডিং (`7:15 - 8:45`)

### 🖥️ স্ক্রিনে যা দেখাবেন:
1. VS Code এ [`backend/src/api/lesson-progress/controllers/lesson-progress.ts`](file:///F:/Learning%20Management%20System%20%28LMS%29/backend/src/api/lesson-progress/controllers/lesson-progress.ts) ওপেন করুন।
2. এরপর [`backend/src/api/quiz/controllers/quiz.ts`](file:///F:/Learning%20Management%20System%20%28LMS%29/backend/src/api/quiz/controllers/quiz.ts) ওপেন করুন (লাইন ১৪৫ থেকে ২০৫)।

### 🗣️ মুখে যা বলবেন (লাইন-বাই-লাইন বাংলায়):
> "এখন আমি মূল কোড লজিকটি লাইন বাই লাইন ব্যাখ্যা করছি।
> 
> প্রথমে [`lesson-progress.ts`](file:///F:/Learning%20Management%20System%20%28LMS%29/backend/src/api/lesson-progress/controllers/lesson-progress.ts) কন্ট্রোলারে দেখুন:
> * **লাইন ৪–৬:** `ctx.state.user` চেক করে রিকোয়েস্টকারীকে অথেন্টিকেট করা হয়।
> * **লাইন ১৭–৩৪:** স্টুডেন্টের পাঠানো লেসন আইডি থেকে ডাটাবেজের `lessonEntity` এবং তার সাথে যুক্ত `course` খুঁজে বের করা হয়।
> * **লাইন ৩৯–৪৯:** ডাটাবেজের `enrollment` টেবিলে চেক করা হয় স্টুডেন্টটি এই কোর্সে এনরোল্ড কি না। এনরোল্ড না থাকলে রিকোয়েস্ট ব্লক করে দেওয়া হয়।
> * **লাইন ৫১–৬১:** `existingProgress` চেক করে ডুপ্লিকেট কমপ্লিশন এন্ট্রি রোধ করা হয়।
> * **লাইন ৬৪–৭২:** সব ভ্যালিডেশন শেষে `strapi.db.query` দিয়ে স্টুডেন্ট আইডি, লেসন আইডি এবং `completed: true` ডাটাবেজে পারসিস্ট করা হয়।
> * **লাইন ৮১–৯৫:** `find` মেথডে ডেটা আইসোলেশন ফিল্টার নিশ্চিত করা হয়েছে।
> 
> এবার [`quiz.ts`](file:///F:/Learning%20Management%20System%20%28LMS%29/backend/src/api/quiz/controllers/quiz.ts) কন্ট্রোলারে আমাদের `submit` মেথডটি দেখুন:
> * **লাইন ১৪৫–১৫৪:** ইউজার ভ্যালিডেশনের পর স্টুডেন্টের সাবমিট করা `answers` অবজেক্ট গ্রহণ করা হয়।
> * **লাইন ১৫৮–১৭৬:** ডাটাবেজ থেকে কুইজ এবং তার সমস্ত `questions` ফেচ করা হয়।
> * **লাইন ১৭৮–১৮৮:** একটি লুপের মাধ্যমে প্রতিটি প্রশ্নের ডাটাবেজে থাকা গোপন `question.correctAnswer`-এর সাথে স্টুডেন্টের উত্তরের তুলনা করা হয় এবং সঠিক হলে `correctCount` ১ বৃদ্ধি করা হয়।
> * **লাইন ১৯০:** `(correctCount / totalQuestions) * 100` সূত্রের মাধ্যমে শতকরা স্কোর হিসাব করা হয়।
> * **লাইন ১৯৩–২০১:** `strapi.db.query('api::quiz-result.quiz-result').create` দিয়ে ফলাফল ডাটাবেজে পারসিস্ট করা হয়।"

---

## 🎬 পর্ব ৭: ডিপ্লয়মেন্ট সেটআপ ও সমাপ্তি (`8:45 - 10:00`)

### 🖥️ স্ক্রিনে যা দেখাবেন:
1. **Railway Dashboard:** Strapi Service &rarr; **Variables** ট্যাবে গিয়ে `DATABASE_CLIENT=postgres`, `DATABASE_URL`, `JWT_SECRET` দেখান।
2. **Vercel Dashboard:** Next.js Project &rarr; **Settings &rarr; Environment Variables** এ গিয়ে `NEXT_PUBLIC_API_URL` দেখান।

### 🗣️ মুখে যা বলবেন (বাংলায়):
> "সর্বশেষে ডিপ্লয়মেন্ট আর্কিটেকচার নিয়ে সংক্ষেপে বলছি:
> * **Railway:** ব্যাকএন্ডের জন্য আমরা Strapi 5-কে Railway-তে ডিপ্লয় করেছি এবং সরাসরি একটি পরিচালিত PostgreSQL ডাটাবেজের সাথে কানেক্ট করেছি। এখানে `DATABASE_CLIENT=postgres` এবং সিকিউর JWT সিক্রেটগুলো এনভায়রনমেন্ট ভেরিয়েবলে কনফিগার করা আছে।
> * **Vercel:** ফ্রন্টএন্ড Next.js অ্যাপটি Vercel-এর গ্লোবাল এজ নেটওয়ার্কে হোস্ট করা এবং `NEXT_PUBLIC_API_URL` এনভায়রনমেন্ট ভেরিয়েবলের মাধ্যমে সরাসরি Railway ব্যাকএন্ড এপিআই-এর সাথে সংযুক্ত।
> 
> প্রজেক্টটির গিটহাবে একটি স্বচ্ছ ও অর্থপূর্ণ কমিট হিস্টোরি রয়েছে এবং রুট `README.md`-তে সমস্ত আর্কিটেকচার ডায়াগ্রাম ও লোকাল সেটআপ গাইড বিস্তারিতভাবে দেওয়া আছে।
> 
> ধৈর্য ধরে পুরো ওয়াকথ্রুটি দেখার জন্য সবাইকে আন্তরিক ধন্যবাদ!"
